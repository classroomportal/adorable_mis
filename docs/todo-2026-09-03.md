# Adorable MIS — To-Do List (updated 3 Sept 2026)

## Done ✅
- **12. Staff roles** — `staff_roles` table (Sysadmin/SMT/Houseparent/HoD/Assessment Manager/Class Teacher), editable on the new **Staff & Roles** admin page (also edit name, code, email; add new staff)
- **2. Teacher logins** — parent contact info now hidden from plain teachers (Pastoral/SMT/admin only); foundation in place, still need to actually invite staff via Supabase Auth
- **3. Assessment Manager access** — only Assessment Managers (+ admin) can write to `results` now
- **4/11. Parent & student portals (foundation only)** — `profiles` can link to a `parent_id`/`student_id`; RLS scoped so each will only ever see their own child's/own data once the login screens exist — **screens themselves not built yet**
- **7. Target grades** — imported (4,746 targets across 224 students), `grade_scale` lookup, red/amber/green "vs Target" badges on the Results page and on each student's page (both inline with results and as a standalone Target Grades card)
- Homepage rebuilt as a tappable dashboard (Students / Whole School / Staff sections)
- Student page sections now collapsible (Core Data stays open, everything else starts collapsed)
- Attendance rebuilt as a proper register (date + period + Mentor-group-first class list)
- Behaviour category dropdown with default points

## Not started yet
1. **SIMS timetable gap** — waiting on your updated SIMS export; re-run the timetable/student importers once it's ready
2. ~~Teacher logins~~ → down to: actually invite real staff accounts via Supabase Auth once emails are in the Staff & Roles page
3. ~~Assessment Manager access~~ → down to: invite Assessment Manager team accounts, confirm Moodle score entry workflow
4. **Parent portal screens** — login, view own child's results/behaviour
5. **Pastoral/SMT behaviour notifications** — auto-alert when a behaviour issue is logged
6. **Auto certificates** at 100/500/1000 behaviour points, printable
8. **Class-level progress view** — is a whole class (e.g. a Maths set) beating or missing target as a group, using the same grade_scale/target_grades foundation
9. **Student photos** on profile, sized to actually recognise faces
10. **Auto Friday detention list** — 10 negative points Sat–Fri triggers inclusion
11. **Student portal screens** — login, view grades/behaviour, appeal negative points → routed to pastoral manager

## Suggested next session order
8 (class progress — quick win, reuses target grades work) → 9 (photos — quick, visible win) → 5/6/10 (behaviour notifications/certificates/detention — natural group) → then the bigger portal build-out (4/11 screens + actual account invites for 2/3).
